# Generated by anchor-configs. SEP-1 (stellar.toml) served at
# http://<host>:8080/.well-known/stellar.toml
NETWORK_PASSPHRASE = "{{NETWORK_PASSPHRASE}}"
SIGNING_KEY = "{{SEP10_SIGNING_PUBLIC_KEY}}"
# Use your public HTTPS URL (via reverse proxy) when deploying.
WEB_AUTH_ENDPOINT = "http://{{HOME_DOMAIN}}:8080/auth"
